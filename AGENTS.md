# Local Instructions

- Default to PowerShell 7.6 via `pwsh` for shell commands in this workspace when practical.
- Keep Windows PowerShell 5.1 usage only for cases that specifically require `powershell.exe`.
- When writing the intelligent-agent research note, use natural paragraphs for body text instead of bullet-list prose.
- Add explanatory text before and/or after each figure and table so visuals are not left without interpretation.
